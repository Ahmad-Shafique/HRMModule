@model IEnumerable<HRM.Entity.SupportingDocument>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SupportingDocumentName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SupportingDocumentLink)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LeaveApplicationId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SupportingDocumentName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SupportingDocumentLink)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LeaveApplicationId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SupportingDocumentId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SupportingDocumentId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SupportingDocumentId })
        </td>
    </tr>
}

</table>
