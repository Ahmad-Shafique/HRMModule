@model IEnumerable<HRM.Entity.Salgrade>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GradeName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LowSalary)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HighSalary)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GradeName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LowSalary)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HighSalary)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SalgradeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SalgradeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SalgradeId })
        </td>
    </tr>
}

</table>
