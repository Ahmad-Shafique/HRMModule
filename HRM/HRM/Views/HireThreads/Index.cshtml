@model IEnumerable<HRM.Entity.HireThread>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Budget)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StartDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HireThreadStatus)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HireRequestId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Budget)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HireThreadStatus)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HireRequestId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.HireThreadId }) |
            @Html.ActionLink("Details", "Details", new { id=item.HireThreadId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.HireThreadId })
        </td>
    </tr>
}

</table>
