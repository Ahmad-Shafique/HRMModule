@model IEnumerable<HRM.Entity.NoticeEmployee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NoticeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NoticeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.NoticeEmployeeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.NoticeEmployeeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.NoticeEmployeeId })
        </td>
    </tr>
}

</table>
