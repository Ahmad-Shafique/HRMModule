@model IEnumerable<HRM.Entity.Interviewee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.InterviewId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TemporaryCVId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.InterviewId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TemporaryCVId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IntervieweeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.IntervieweeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IntervieweeId })
        </td>
    </tr>
}

</table>
