@model IEnumerable<HRM.Entity.CircularTemplate>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TemplateName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Template)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TemplateName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Template)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CircularTemplateId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CircularTemplateId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CircularTemplateId })
        </td>
    </tr>
}

</table>
