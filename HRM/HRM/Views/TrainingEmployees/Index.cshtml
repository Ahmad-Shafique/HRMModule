@model IEnumerable<HRM.Entity.TrainingEmployee>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TrainingId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TrainingId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TrainingEmployeeId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TrainingEmployeeId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TrainingEmployeeId })
        </td>
    </tr>
}

</table>
