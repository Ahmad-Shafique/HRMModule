@model IEnumerable<HRM.Entity.Bonuses>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.BonusId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BonusValue)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BonusDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BonusesDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.BonusId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BonusValue)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BonusDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BonusesDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BonusesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.BonusesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BonusesId })
        </td>
    </tr>
}

</table>
