@model IEnumerable<HRM.Entity.Attendance>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmployeeId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MonthName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Value)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MonthlyAbsence)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmployeeId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MonthName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MonthlyAbsence)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AttendanceId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AttendanceId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AttendanceId })
        </td>
    </tr>
}

</table>
