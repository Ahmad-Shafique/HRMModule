@model IEnumerable<HRM.Entity.DepartmentDesignation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DepartmentId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DesignationId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DepartmentId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DesignationId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DepartmentDesignationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DepartmentDesignationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DepartmentDesignationId })
        </td>
    </tr>
}

</table>
