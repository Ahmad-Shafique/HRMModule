@model IEnumerable<HRM.Entity.TransportArea>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AreaName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AreaDemand)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AssignedCapacity)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AreaName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AreaDemand)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AssignedCapacity)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TransportAreaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.TransportAreaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TransportAreaId })
        </td>
    </tr>
}

</table>
