@model IEnumerable<HRM.Entity.LeaveApplicationCategory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.LeaveApplicationCategoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.LeaveApplicationCategoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LeaveApplicationCategoryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LeaveApplicationCategoryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LeaveApplicationCategoryId })
        </td>
    </tr>
}

</table>
